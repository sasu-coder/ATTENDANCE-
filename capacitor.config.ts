import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.university.attendsecure',
  appName: 'AttendSecure',
  webDir: 'dist'
};

export default config;
